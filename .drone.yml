---
kind: pipeline
name: default

steps:
- name: build
  image: d.xr.to/eater/flavors
  privileged: true
  commands:
    - img login -u "drone" -p "$PASSWORD" d.xr.to
    - ./flavor -ip all
  environment:
    PASSWORD:
      from_secret: pass
